#!/bin/bash
hardpath=`readlink $0`
if [ -z "$hardpath" ]; then
	hardpath="$0"
fi
cd `dirname $hardpath`/../root/css
while true; do
  inotifywait -r -e modify,create,close_write,move,delete . --exclude ".*___jb_bak___$";
  $1 ../../deploy/deploy_css.php
  sleep 1
done
