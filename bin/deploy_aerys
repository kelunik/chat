#!/usr/bin/env bash
hardpath=`readlink $0`
if [ -z "$hardpath" ]; then
	hardpath="$0"
fi
cd `dirname $hardpath`/..
sudo git reset --hard
sudo git pull
sudo composer install
sudo php deploy/deploy.php
