#!/bin/bash

for png in `find $1 -name "*.png"`;
do
	echo -n " Crushing $png "
	pngcrush -reduce -blacken -brute "$png" /tmp/crushed.png 2>&1 | grep "filesize reduction"  | sed -e 's/^ *//' -e 's/ *$//'
	mv -f /tmp/crushed.png $png
	echo -e ' \033[32m✔\033[39m'
done;
