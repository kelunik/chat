#!/usr/bin/env bash
hardpath=`readlink $0`
if [ -z "$hardpath" ]; then
	hardpath="$0"
fi
# specify -D option if you don't want to attach...
sudo screen -D -m -c `dirname $hardpath`/../config/screenrc.txt `dirname $hardpath`/init_aerys
